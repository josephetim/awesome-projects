# TROUBLESHOOT

## DNS lookup fails

- Verify internet access and DNS resolver configuration.
- Try a known domain like `example.com`.

## TCP connection errors

- Remote host/port may be closed or filtered.
- Connection failure is expected for many targets and still useful for learning.

## HTTP request timeout

- Increase timeout in code for slow networks.
- Confirm URL scheme is `http://` or `https://`.
