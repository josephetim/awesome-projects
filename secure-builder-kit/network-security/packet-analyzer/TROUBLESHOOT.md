# TROUBLESHOOT

## Permission denied

- Run with elevated privileges (`sudo` on Linux/macOS).
- On Windows, run terminal as Administrator with proper capture driver support.

## Interface not found

- List interfaces and confirm exact interface name.
- Verify adapter is up and active.

## No packets appearing

- Generate local traffic (for example open a web page or ping a host).
- Confirm capture interface matches active network path.
